name: Update IPTV JSON List
on:
  workflow_dispatch:
  schedule:
    - cron: '*/20 * * * *'

jobs:
  generate-json:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3

      - name: Fetch and Convert M3U to JSON
        run: |
          curl -s -L "https://raw.githubusercontent.com/minhtan001/IPTV-Auto/refs/heads/playlists/all.m3u" > raw.m3u
          
          cat << 'EOF' > convert.py
          import json
          import re

          data = {
            "id": "HFB",
            "url": "http://hfb123.netlify.app/MT.json",
            "name": "HFB",
            "color": "#1cb57a",
            "description": "HFB - Trang web phát sóng bóng đá trực tuyến miễn phí hàng đầu tại Việt Nam.",
            "image": {
              "url": "https://t4.ftcdn.net/jpg/02/11/51/53/360_F_211515361_bnIbyKadClzn3hJT0zCPPuPApcG7k3lC.jpg"
            },
            "groups": [
              {
                "id": "live",
                "name": "🔴 Live",
                "display": "horizontal",
                "grid_number": 2,
                "channels": [],
                "remote_data": {
                  "url": "http://tt.8share.pro/buncha/group/live"
                }
              }
            ]
          }

          try:
              with open('raw.m3u', 'r', encoding='utf-8') as f:
                  lines = [line.strip() for line in f if line.strip()]
          except Exception as e:
              lines = []
              print("Lỗi đọc file:", e)

          channels = []
          # Thêm bộ đếm để tạo ID duy nhất
          channel_counter = 1

          for i in range(len(lines)):
              line = lines[i]
              
              if line.startswith('#EXTINF') and 'Socolive' in line:
                  parts = line.split(',', 1)
                  if len(parts) > 1:
                      display_text = parts[1].strip()
                      
                      match_info = re.match(r'(.*?):\s*\[(.*?)\]\s*(.*)', display_text)
                      
                      if match_info:
                          time_str = match_info.group(1).strip()
                          blv_name = match_info.group(2).strip()
                          match_name = match_info.group(3).strip()
                      else:
                          time_str = "Live"
                          blv_name = "BLV Socolive"
                          match_name = display_text

                      if i + 1 < len(lines) and lines[i+1].startswith('http'):
                          raw_url = lines[i+1]
                          id_match = re.search(r'id=(\d+)', raw_url)
                          
                          if id_match:
                              stream_id = id_match.group(1)
                              new_stream_url = f"https://live.inplyr.com/room/{stream_id}.m3u8"
                              
                              # SỬ DỤNG BỘ ĐẾM ĐỂ TẠO ID DUY NHẤT
                              unique_channel_id = f"socolive-{stream_id}-{channel_counter}"
                              
                              channel_obj = {
                                "id": unique_channel_id,
                                "name": match_name,
                                "labels": [
                                  { "position": "bottom-left", "text": blv_name, "color": "#0066CC", "text_color": "#FFFFFF" },
                                  { "position": "top-left", "text": "● Live", "color": "#FF0000", "text_color": "#FFFFFF" },
                                  { "position": "center", "text": time_str, "color": "#0066CC", "text_color": "#FFFFFF" }
                                ],
                                "description": f"Socolive • {time_str}",
                                "image": {
                                  "url": "https://raw.githubusercontent.com/nghehoang007-wq/HFB321/main/HFB/Thump/nguonphat1.png",
                                  "height": 480, "width": 640, "display": "cover", "shape": "square"
                                },
                                "type": "single",
                                "display": "thumbnail-only",
                                "sources": [
                                  {
                                    "id": f"source-{unique_channel_id}",
                                    "name": "Socolive",
                                    "contents": [
                                      {
                                        "id": f"content-{unique_channel_id}",
                                        "name": match_name,
                                        "streams": [
                                          {
                                            "id": f"stream-{unique_channel_id}",
                                            "name": blv_name,
                                            "stream_links": [
                                              {
                                                "id": f"link-{unique_channel_id}",
                                                "name": "HLS",
                                                "type": "hls",
                                                "default": True,
                                                "url": new_stream_url,
                                                "request_headers": [
                                                  { "key": "Referer", "value": "https://bunchatv3.com/" }
                                                ]
                                              }
                                            ]
                                          }
                                        ]
                                      }
                                    ]
                                  }
                                ]
                              }
                              channels.append(channel_obj)
                              # Tăng bộ đếm
                              channel_counter += 1

          data['groups'][0]['channels'] = channels

          with open('MT.json', 'w', encoding='utf-8') as f:
              json.dump(data, f, ensure_ascii=False, indent=2)
          EOF
          
          python3 convert.py
          
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add MT.json
          git commit -m "Fix duplicate IDs in JSON" || echo "No changes to commit"
          git push
