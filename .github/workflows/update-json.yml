name: Update IPTV JSON List
on:
  workflow_dispatch:
  schedule:
    - cron: '*/20 * * * *'

jobs:
  generate-json:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3

      - name: Fetch and Convert M3U to JSON
        run: |
          curl -s -L "https://raw.githubusercontent.com/minhtan001/IPTV-Auto/refs/heads/playlists/all.m3u" > raw.m3u
          
          cat << 'EOF' > convert.py
          import json
          import re

          # Hàm tự động ngắt dòng thông minh (mặc định 12 ký tự/dòng)
          def wrap_text(text, max_len=12):
              words = text.split()
              lines = []
              curr_line = ""
              for word in words:
                  if len(curr_line) + len(word) <= max_len:
                      curr_line += word + " "
                  else:
                      if curr_line:
                          lines.append(curr_line.strip())
                      curr_line = word + " "
              if curr_line:
                  lines.append(curr_line.strip())
              # Thêm \n để xuống dòng, Monplayer sẽ tự động căn giữa (center align)
              return "\n".join(lines)

          # Hàm lấy thời gian để sắp xếp lịch phát sóng
          def get_sort_key(t_str):
              m = re.search(r'(\d{1,2})[hH:](\d{2})(?:-(\d{1,2})/(\d{1,2}))?', t_str)
              if m:
                  hour = int(m.group(1))
                  minute = int(m.group(2))
                  day = int(m.group(3)) if m.group(3) else 0
                  month = int(m.group(4)) if m.group(4) else 0
                  return (month, day, hour, minute)
              return (99, 99, 99, 99) 

          data = {
            "id": "HFB",
            "url": "http://hfb123.netlify.app/MT.json",
            "name": "HFB",
            "color": "#1cb57a",
            "description": "HFB - Trang web phát sóng bóng đá trực tuyến.",
            "image": {
              "url": "https://t4.ftcdn.net/jpg/02/11/51/53/360_F_211515361_bnIbyKadClzn3hJT0zCPPuPApcG7k3lC.jpg"
            },
            "groups": [
              {
                "id": "live",
                "name": "🔴 Lịch Phát Sóng",
                "display": "vertical",
                "grid_number": 2,
                "channels": []
              }
            ],
            "sorts": [],
            "notice": {
              "id": "notice",
              "link": "",
              "icon": "",
              "closeable": True
            },
            "option": {
              "save_history": False,
              "save_search_history": False,
              "save_wishlist": False
            }
          }

          try:
              with open('raw.m3u', 'r', encoding='utf-8') as f:
                  lines = [line.strip() for line in f if line.strip()]
          except Exception as e:
              lines = []

          channels_with_sort_key = []
          uid = 1

          for i in range(len(lines)):
              line = lines[i]
              
              if line.startswith('#EXTINF') and 'Socolive' in line:
                  parts = line.split(',', 1)
                  if len(parts) > 1:
                      display_text = parts[1].strip()
                      
                      # Tách Giờ, BLV, Tên Trận
                      match_info = re.match(r'(.*?):\s*\[(.*?)\]\s*(.*)', display_text)
                      
                      if match_info:
                          time_str = match_info.group(1).strip()
                          blv_name = match_info.group(2).strip()
                          raw_match_name = match_info.group(3).strip()
                      else:
                          time_str = "00h00"
                          blv_name = "BLV Socolive"
                          raw_match_name = display_text

                      if i + 1 < len(lines) and lines[i+1].startswith('http'):
                          raw_url = lines[i+1]
                          id_match = re.search(r'id=(\d+)', raw_url)
                          
                          if id_match:
                              stream_id = id_match.group(1)
                              new_stream_url = f"https://live.inplyr.com/room/{stream_id}.m3u8"
                              unique_match_name = f"{raw_match_name} ({blv_name})"
                              
                              # Gắn nhãn mặc định: Giờ (Giữa) và Tên BLV (Dưới Trái)
                              labels_list = [
                                  { "position": "bottom-left", "text": blv_name, "color": "#0066CC", "text_color": "#FFFFFF" },
                                  { "position": "center", "text": time_str, "color": "#FF0000", "text_color": "#FFFFFF" }
                              ]
                              
                              # Bóc tách 2 đội từ chữ "vs" (không phân biệt chữ hoa/thường)
                              teams = re.split(r'(?i)\s+vs\s+', raw_match_name, maxsplit=1)
                              if len(teams) == 2:
                                  team1 = teams[0].strip()
                                  team2 = teams[1].strip()
                                  
                                  # CHUẨN HÓA VỊ TRÍ: Dùng center-left và center-right
                                  # Dùng màu #80000000 (Đen trong suốt 50%) để chữ không bị chìm vào nền ảnh
                                  labels_list.append({ "position": "center-left", "text": wrap_text(team1), "color": "#80000000", "text_color": "#FFFFFF" })
                                  labels_list.append({ "position": "center-right", "text": wrap_text(team2), "color": "#80000000", "text_color": "#FFFFFF" })
                              else:
                                  # Nếu không có chữ vs, hiển thị toàn bộ tên lên góc trên trái
                                  labels_list.append({ "position": "top-left", "text": wrap_text(raw_match_name, 20), "color": "#80000000", "text_color": "#FFFFFF" })

                              # Cấu trúc 1 trận đấu
                              channel_obj = {
                                "id": f"ch-{uid}",
                                "name": unique_match_name,
                                "labels": labels_list,
                                "description": f"Socolive • {time_str}",
                                "image": {
                                  "url": "https://raw.githubusercontent.com/nghehoang007-wq/HFB321/main/HFB/Thump/nguonphat.png",
                                  "height": 480, "width": 640, "display": "cover", "shape": "square"
                                },
                                "type": "single",
                                "display": "thumbnail-only", # Đã đưa về ảnh Thumbnail tinh gọn
                                "sources": [
                                  {
                                    "id": f"src-{uid}",
                                    "name": "Socolive",
                                    "contents": [
                                      {
                                        "id": f"cnt-{uid}",
                                        "name": unique_match_name,
                                        "streams": [
                                          {
                                            "id": f"strm-{uid}",
                                            "name": blv_name,
                                            "stream_links": [
                                              {
                                                "id": f"lnk-{uid}",
                                                "name": "HLS",
                                                "type": "hls",
                                                "default": True,
                                                "url": new_stream_url,
                                                "request_headers": [
                                                  { "key": "Referer", "value": "https://bunchatv3.com/" }
                                                ]
                                              }
                                            ]
                                          }
                                        ]
                                      }
                                    ]
                                  }
                                ]
                              }
                              
                              sort_key = get_sort_key(time_str)
                              channels_with_sort_key.append((sort_key, channel_obj))
                              uid += 1

          # Sắp xếp các luồng theo giờ
          channels_with_sort_key.sort(key=lambda x: x[0])
          
          # Đưa kết quả vào JSON
          data['groups'][0]['channels'] = [item[1] for item in channels_with_sort_key]

          with open('MT.json', 'w', encoding='utf-8') as f:
              json.dump(data, f, ensure_ascii=False, indent=2)
          EOF
          
          python3 convert.py
          
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add MT.json
          git commit -m "UI: Add symmetric multiline labels with Hex colors" || echo "No changes to commit"
          git push
